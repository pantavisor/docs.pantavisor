apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pv-prod-docs-mcp-server
  namespace: default
  annotations:
    # Placeholders — adjust to match the cluster's actual ingress class and
    # cert-manager ClusterIssuer name before applying.
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - mcp.docs.pantavisor.io
      secretName: docs-mcp-server-tls
  rules:
    - host: mcp.docs.pantavisor.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pv-prod-docs-mcp-server
                port:
                  number: 80
