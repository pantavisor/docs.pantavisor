import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// Generated, versioned reference docs (repo-shaped: meta-pantavisor, pantavisor,
// pvr). Autogenerated from the folder structure produced by migrate-docs.js.
//
// Two independent sidebars, each scoped to its own top-level directory, so
// browsing /reference/pantavisor only shows the pantavisor tree and
// /reference/meta-pantavisor only shows the meta-pantavisor tree — instead of
// one shared sidebar showing both at once. Docusaurus assigns each doc to
// whichever sidebar's generated tree contains it.
//
// Keys must stay in sync with the SIDEBAR_STUB in scripts/sync-reference.mjs,
// which writes the matching frozen-version sidebar files.
const sidebars: SidebarsConfig = {
  pantavisorSidebar: [{type: 'autogenerated', dirName: 'pantavisor'}],
  metaPantavisorSidebar: [{type: 'autogenerated', dirName: 'meta-pantavisor'}],
};

export default sidebars;
